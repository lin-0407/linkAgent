# Lumen Agent — 项目启动交接文档

> 本文档用于将作者在另一个 Claude 会话中讨论确定的项目蓝图，交接给新仓库的 Claude Code 助手。阅读本文档后，你应当完全理解项目目标、架构与协作方式，并可直接进入阶段 0 的落地工作。

---

## 0. 协作约定（请严格遵守）

1. **学习模式（Learning Style）**：作者正处于技术成长期，你不是"全代人"而是"教练 + 搭档"。每当你要写 20+ 行涉及**核心设计决策**的代码时，必须：
   - 先搭建上下文骨架
   - 留下唯一的 `TODO(human)` 标记
   - 按以下格式请求作者亲手实现：
     ```
     ● Learn by Doing
     Context: 已构建什么、为什么这个决策重要
     Your Task: 在 xxx.java 的 xxx 方法中实现，找 TODO(human)
     Guidance: 需要权衡的点与约束
     ```
   - 然后停下来等待作者完成，不要替他写。
2. **Insight 习惯**：关键实现前后用以下格式给 2-3 条代码洞察（非泛泛教程，聚焦当前代码与架构）：
   ```
   ★ Insight ─────────────────────────────────────
   ...
   ─────────────────────────────────────────────────
   ```
3. **原则**：少写注释、不做过度设计、先跑通再优化、不要引入没用到的抽象。
4. **语言**：中文回复，代码英文命名。

---

## 1. 作者画像

- Java 后端方向学习者，已有 Spring Boot / Spring AI 使用经验
- 职业目标：成为 **"AI 应用开发 + 后端开发"** 复合型工程师
- 本项目定位：**作品集项目（找工作导向）**
- 技术栈偏好：主栈 Java + Spring AI（差异化），Python + LangGraph 留作后续第二作品

---

## 2. 项目定位

**名称**：`link-agent`（可按作者意愿更名）

**一句话定位**：
> 基于 Spring AI 的企业级通用 ReAct Agent 框架，支持多步规划、工具调用、会话记忆、可观测与可扩展工具生态。

**简历卖点**（倒推所有技术选型）：
1. 自主实现 ReAct 编排内核（**不依赖 LangChain4j 等现成 Agent 框架**，体现原理掌握）
2. 企业级能力：分布式会话、异步流式、链路追踪、降级重试
3. 工具生态：内置工具 + **MCP 协议兼容**（前沿标准）
4. 可观测：Token 成本统计、Trace 可视化
5. 有可演示的 Demo 场景（深度研究 Agent / 个人任务管家）

---

## 3. 目标架构

```
┌──────────────────────────────────────────────┐
│  接入层  REST / SSE Stream / WebSocket        │
├──────────────────────────────────────────────┤
│  Agent 编排层 (核心)                          │
│   ├─ AgentExecutor   ReAct 主循环             │
│   ├─ Planner         规划器（单步 / 多步）    │
│   ├─ ToolDispatcher  工具路由 + 并发执行      │
│   └─ MemoryManager   短期 + 长期 + 摘要       │
├──────────────────────────────────────────────┤
│  能力层                                       │
│   ├─ Tools (内置 + MCP)                       │
│   ├─ LLM Router (Qwen / DeepSeek / Claude)    │
│   └─ RAG (Hybrid + Rerank)                    │
├──────────────────────────────────────────────┤
│  基础层 Redis / MySQL / VectorDB / Prometheus │
└──────────────────────────────────────────────┘
```

**推荐包结构**：
```
com.lumen.agent
├── core              # AgentExecutor, AgentContext, AgentStep, ToolCall, Observation
├── planner           # ReActPlanner, PlanAndExecutePlanner, Reflection
├── tool              # ToolRegistry, builtin/*, mcp/*, sandbox/*
├── memory            # ShortTermMemory, SummaryMemory, LongTermMemory
├── llm               # LLMRouter, QwenClient, ClaudeClient
├── rag               # HybridRetriever, Reranker, AgenticRAG
├── observability     # TraceRecorder, MetricsCollector
└── api               # Controller, DTO, SSE
```

---

## 4. 技术选型

| 层 | 选型 | 备注 |
|---|---|---|
| 框架 | Spring Boot 3.x + Spring AI 1.0 | 主栈 |
| JDK | 17+ | Spring AI 要求 |
| LLM | Qwen3 / DeepSeek-V3（国内可用）+ Claude（可选） | 多模型路由 |
| 向量库 | PgVector（简单）或 Milvus（性能） | 二选一 |
| 记忆存储 | Redis（短期） + MySQL（长期） | |
| 可观测 | Langfuse（开源自部署） | Docker 起即可 |
| MCP | `io.modelcontextprotocol:java-sdk` | 前沿协议 |
| 前端 | Vue3 + SSE，或 Chainlit/Streamlit | 不是重点，能演示即可 |
| 部署 | Docker Compose + 阿里云轻量服务器 | 公网 Demo 是加分项 |

---

## 5. 12 周分阶段路线图

每阶段结束作为一次可提交 commit / PR，形成清晰的 git 历史。

### 阶段 0：项目奠基（第 1 周）
- 初始化 Spring Boot 3 + Spring AI 项目骨架
- 建立上述包结构
- 配置 Qwen / DeepSeek API Key、Redis、MySQL、PgVector
- 写好 README：项目定位、架构图、路线图
- 跑通一个最简 `chatClient.prompt().call()` 作为 Hello World

### 阶段 1：ReAct 最小闭环 ⭐（第 2-3 周）
- 定义核心抽象：`AgentContext`, `AgentStep`, `ToolCall`, `Observation`
- 实现 `AgentExecutor.run()` 主循环（Thought → Action → Observation → loop）
- 实现 `ReActPrompt` 模板
- 接入 3 个基础工具：`web_search`、`calculator`、`datetime`
- **🎯 Learn by Doing**：主循环终止条件与最大迭代保护
- 目标：问 "北京今天天气如何，摄氏度转华氏度是多少" 能自主调 2 个工具得出答案

### 阶段 2：记忆系统（第 4 周）
- 短期记忆：滑动窗口（Spring AI ChatMemory 定制）
- 摘要记忆：超过 token 阈值自动摘要
- 长期记忆：向量库存事实 + 用户画像
- **🎯 Learn by Doing**：摘要触发策略（何时压缩、压缩多少）

### 阶段 3：工具生态（第 5-6 周）
- 工具注册机制：`@Tool` 注解 + SPI + 运行时注册
- **并发工具调用**（Reactor 并行，Claude / GPT 的 parallel tool use 等价物）
- 超时、重试、降级
- **MCP 适配器**（连接外部 MCP Server，重要卖点）
- 代码执行沙箱（可选，若做 Coding Agent 方向）

### 阶段 4：RAG 升级（第 7 周）
- Hybrid Search：向量 + BM25
- Reranker：BGE-Reranker-v2
- Agentic RAG：LLM 决定是否 / 如何检索
- **🎯 Learn by Doing**：混合检索结果融合（RRF 或加权）

### 阶段 5：Planner & Reflection（第 8-9 周）
- Plan-and-Execute 模式（与 ReAct 对比）
- Reflection：失败后自我反思重试
- 子任务拆分与汇总
- 可选：多 Agent 协作（Planner + Worker）

### 阶段 6：可观测性（第 10 周）
- Trace：完整执行步骤链存 DB + 前端可视化
- 指标：Token 消耗、工具调用、迭代次数、失败率
- 接入 Langfuse 或自研简易 Trace UI
- Prometheus + Grafana

### 阶段 7：Demo 与打磨（第 11-12 周）
- 前端页面（简单但不陋）
- 2-3 个 Demo 场景：**推荐深度研究 Agent + 个人任务管家**
- Docker Compose 一键部署
- 公网部署 + README 精修 + 演示视频

---

## 6. 作者亲手实现清单（Learning 风格承诺）

以下 5 个模块**不要替作者写**，必须留 `TODO(human)` 并用 Learn by Doing 格式请求：

1. ReAct 主循环的**终止判定**（阶段 1）
2. 摘要记忆的**触发策略**（阶段 2）
3. 混合检索的**结果融合**（阶段 4）
4. Reflection 的**触发与重试决策**（阶段 5）
5. Trace 的**数据结构设计**（阶段 6）

---

## 7. 项目纪律（反模式清单）

- ❌ 不要使用 LangChain4j 的 AiServices 自动代理（这会遮蔽 ReAct 内核的实现，失去卖点）
- ❌ 不要引入电商 / 菜单 / 订单等业务代码（这是旧项目的包袱）
- ❌ 不要在没有实际用例前加 feature flag、抽象接口、工厂模式
- ❌ 不要写长注释文档；README 讲清楚，代码命名讲清楚
- ❌ 不要 "一把梭" 把整个阶段代码都写完 —— 必须在关键决策点停下来让作者写

---

## 8. 第一次对话建议

新仓库的 Claude 读完本文档后，建议按以下顺序开场：

1. 确认已理解项目定位、架构、协作约定
2. 询问作者是否已初始化 Spring Boot 项目（如未，先做项目脚手架）
3. 进入**阶段 0**：搭建包结构、配置依赖、跑通 Hello World
4. 阶段 0 完成后进入**阶段 1**，并在主循环终止判定处停下，请求作者亲手实现

---

## 9. 状态快照

- **交接日期**：2026-04-24
- **当前阶段**：阶段 0 开始前
- **作者所在仓库**：新建空仓库（名称待定）
- **上个仓库（可选参考）**：`link-local-service-center`（已决定废弃其业务代码，但 Spring AI 配置可借鉴）

---

> 请以本文档为唯一真相来源。如与作者当前指令冲突，以作者当前指令为准，但需提醒作者是否要同步更新本文档。
