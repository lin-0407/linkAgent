# 阶段 5.2 - 高级检索链路（设计方案；5.2a 已实施）

> 承接 5.1「跨分区视频案例知识库（存储底座）」。本文件是阶段 5 第二个子阶段的**需求分析 + 设计方案**，按项目流程，**自检确认后才进入编码**。
> 5.1 现状准绳见 `docs/develop/阶段5.1-跨分区视频案例知识库.md` §7 与 `docs/reference/跨分区视频案例知识库说明.md`。

---

## 0. 作者已拍板（2026-06-07）

| 维度 | 决策 | 落地含义 |
|---|---|---|
| 推进方式 | **增量切片，先最小闭环** | 像 5.1 那样拆 5.2a/b/c/d/e，每片可独立编译自测、单一职责 |
| 检索入口 | **`/search` 接口 + 前端检索面板** | 5.2a 就交付可 curl 自测的接口 + 前端最小检索 UI（像 5.1 把案例库 surface 提前） |
| 混合检索 | **Milvus 原生 dense+sparse(BM25) hybrid** | 走正统路线（非 Java 层 RRF），用 milvus-sdk-java 2.5.x 原生 `hybridSearch`，作为单独一刀 |
| 父子召回 | **本轮就做子表向量化 + 父子召回** | 子表（优质评论弹幕）向量化 + small-to-big，纳入 5.2 范围（不下推 5.3） |

> 这四条共同意味着：**5.2 整体是“全做”，但按切片逐刀推进**；本文件详设第一刀 5.2a，其余刀给出已联网核实的路线与“必须先验证”项。

---

## 1. 需求分析

- **目标用户 / 场景**：B 站中小 UP 主。同一套跨分区案例库，被未来 Agent（5.3）在「发布前优化 / 竞品分析」时检索，给建议注入“同赛道里被验证过的成功视频”作证据。5.2 本身先把**检索能力**做出来并可独立自测。
- **要解决的痛点**：5.1 只把案例**存进去、向量化**了，但还**没有“查”的能力**。`index/status` 里的 `retrievalMode` 目前只是“预测值”，没有真正的检索本体。
- **输入**：一段自然语言查询 `query`（如“知识区高完播的开场怎么做”）+ 可选过滤 `category` / `tier` / `topK`。
- **输出**：按相关性排序的案例候选列表（父表案例卡片 + 命中的子条目证据）+ **本次实际走了哪种检索模式**（VECTOR / SQL / 混合 / 降级）。
- **本阶段做**：把 5.1 的存储底座变成**可检索链路**，并暴露 `/search` 接口与前端检索面板。完整链路 = 查询改写/多查询/HyDE → 原生混合(BM25)+RRF → Rerank → 父子召回 → SQL 兜底，**按 5.2a–e 逐刀落地**。
- **本阶段不做**：Agent 工具化与接入发布前优化/竞品（5.3）、知识库完整管理后台（5.4）、跨创作者权限隔离、看视频内容评质量的多模态。
- **降级红线（继承 5.1）**：`knowledge.rag.enabled=false` 或向量库未就绪时，检索**走 SQL 兜底且不报错**，沿用反馈侧 `CreatorFeedbackEvidenceRetrievalService` 的成熟范式。

---

## 2. 5.1 现状地基（5.2 不重复造）

| 资产 | 现状 | 对 5.2 的意义 |
|---|---|---|
| 父表 `creator_reference_video` | 每视频 1 张案例卡片，已可被 5.1c 索引 | 5.2a 语义检索的**召回单元**与回查事实源 |
| 子表 `creator_reference_video_item` | 已存清洗后优质正/负向评论弹幕，**未向量化** | 5.2c 父子召回的 small 端，需新增向量化 |
| `KnowledgeVectorStore`（隔离 Bean） | 内部私有持 `MilvusServiceClient`(v1) + `MilvusVectorStore`；`getVectorStore()` 暴露 dense 检索 | 5.2a 走 `MilvusVectorStore.similaritySearch`；5.2d 原生 hybrid 走内部 raw client |
| 父表向量 metadata | 5.1c 已写入 `videoId/bvId/tier/category/source/qualityScore/各热度/publishTimeText` | 5.2a 的 `category/tier` **元数据过滤**直接可用 |
| 反馈检索范式 | `向量召回 id → MySQL 回查 is_deleted=0 → 命中不足合并 SQL` 三段式 | 5.2a 的**回查 + 降级**直接照搬这套，已验证可用 |
| `retrievalMode` 字面量 | `index/status` 已用 `VECTOR`/`SQL` | 5.2 检索响应沿用同一套，新增 `VECTOR_WITH_SQL_FALLBACK`/`HYBRID` |

> 关键约束：知识库向量库**不是** `VectorStore` 类型的 Spring Bean（隔离设计），5.2 一切检索必须经 `KnowledgeVectorStore`，**不能**注入 `ObjectProvider<VectorStore>`（那是反馈侧的集合）。

---

## 3. 增量切片路线图（5.2a–e）

| 切片 | 目标 | 主要产出 | 动存储/索引? | 依赖 |
|---|---|---|---|---|
| **5.2a（本次详设）** | **最小检索闭环** | dense 语义检索（父表）+ SQL 兜底 + `POST /search` + 前端检索面板 + 模式回显 | 否（复用 5.1c 父表索引） | 无 |
| 5.2b | 查询理解 | 查询改写 / 多查询 / HyDE（LLM 扩展查询，纯加在检索前） | 否 | 5.2a |
| 5.2c | 父子召回 | 子表向量化 + small-to-big（child 召回 → 扩展回父表卡片） | **是**（新增子表向量索引） | 5.2a |
| 5.2d | 原生混合检索 | Milvus 原生 dense+sparse(BM25) hybrid + RRF 融合 | **是**（知识库集合改自建 schema） | 5.2a；**Milvus server ≥ 2.5** |
| 5.2e | 重排序 | Rerank（DashScope 重排模型）对融合结果精排 | 否 | 5.2c/5.2d |

> 顺序理由：先把“能查”跑通（a），再加“查得准”的查询侧（b），再补“小召大”的父子（c），再上“正统混合”（d，最重、要迁移集合 schema），最后精排（e）。每刀都能独立编译 + curl 自测，符合单一职责。

---

## 4. 5.2a 详细设计（第一刀，本次编码目标）

### 4.1 范围界定（只做这些）

dense 语义检索（查父表已索引的案例卡片）→ `videoId` 回查父表事实源 → 命中不足/向量库不可用时 SQL 关键词兜底 → 返回候选 + 实际检索模式。**不含**改写、混合、Rerank、子表/父子召回——那些是 b/c/d/e。

### 4.2 包与类（简单优先：先放 knowledge 模块内，不提前建空 `rag` 抽象）

> 5.1 设计曾预留 `com.link.linkagent.rag` 作“通用 RAG 内核”。但 5.2 检索完全是“知识案例库”专用逻辑，**此刻没有第二个消费方**。按“不提前建空抽象 / 简单优先”，5.2a 先把检索放在 `knowledge.service`；待 5.3 Agent 内核统一、出现复用时，再把 RRF/查询改写等**共性能力**上提到 `rag` 内核。**此点请作者自检定夺**（若希望现在就进 `rag` 包，我按你定的来）。

| 新增/改动 | 类型 | 职责 |
|---|---|---|
| `knowledge.service.KnowledgeRetrievalService` | 新增 Service | 检索编排：dense 召回 → 回查 → SQL 兜底 → 组装结果 + 模式 |
| `knowledge.controller.KnowledgeReferenceVideoController` | 改动 | 加 `POST /search` 一个方法（复用现有控制器，不新建） |
| `knowledge.model.ReferenceVideoSearchRequest` | 新增 record | 入参：`query` / `category?` / `tier?` / `topK?` |
| `knowledge.model.ReferenceVideoSearchResponse` | 新增 record | 出参：`mode` + `items[]` |
| `knowledge.model.ReferenceVideoSearchItem` | 新增 record | 单条候选：父表展示字段 + 相似度/命中说明 |
| `knowledge.mapper.KnowledgeReferenceVideoMapper` | 改动 | 加 2 个查询：①按 `videoId` 批量回查父表；②SQL 关键词兜底检索父表 |
| `knowledge.config.KnowledgeRagProperties` | 改动 | 加检索参数 `topK`(默认 8) / `minVectorHitCount`(默认 3) |

### 4.3 检索编排流程（照搬反馈侧三段式）

```
search(query, category, tier, topK):
  ragEnabled = knowledge.rag.enabled
  vectorStore = ragEnabled && KnowledgeVectorStore.isReady() ? getVectorStore() : null
  if vectorStore == null:
      return { mode: SQL, items: sqlKeywordFallback(query, category, tier, topK) }   // 优雅降级，不报错

  try:
      orderedVideoIds = denseSearch(query, category, tier, topK)   // similaritySearch + 元数据过滤
  catch 运行期异常(连接/维度/超时):
      log.warn; return { mode: SQL, items: sqlKeywordFallback(...) }   // 不中断，降级

  records = mapper.listByVideoIds(orderedVideoIds)   // 回查父表 is_deleted=0，向量库脏数据被过滤
  records = 按 orderedVideoIds 顺序重排(records)      // MySQL IN 不保证顺序

  if records.size() >= minVectorHitCount:
      return { mode: VECTOR, items: top(records, topK) }
  else:
      merged = 向量结果在前 + SQL 兜底补后，按 videoId 去重
      return { mode: VECTOR_WITH_SQL_FALLBACK, items: top(merged, topK) }
```

- `denseSearch`：`SearchRequest.builder().query(query).topK(topK).filterExpression(filter).build()` → `vectorStore.similaritySearch(...)`，从 `Document.metadata.videoId`（回退 `Document.id`）取 `videoId`。`filter` 用 `FilterExpressionBuilder` 按 `category`/`tier` 组合（与反馈侧按 `taskId` 过滤同款，**不手工拼字符串**）。
- **回查事实源**：向量库只给 `videoId`，父表才是真身；`is_deleted=0` 过滤掉旧批次/已删案例。
- **模式回显**：`VECTOR` / `SQL` / `VECTOR_WITH_SQL_FALLBACK` 三态，复用反馈侧字面量；给前端展示“本次走了哪条路”。

### 4.4 SQL 关键词兜底（父表，新增 mapper 查询）

5.1 的 `listReferenceVideos` 只按 `category/tier` 过滤、按 `id` 倒序，**没有关键词检索**。5.2a 新增：

```sql
SELECT <父表展示列>
FROM creator_reference_video
WHERE is_deleted = 0
  AND (#{category} IS NULL OR category = #{category})
  AND (#{tier}     IS NULL OR tier = #{tier})
  AND ( #{kw} IS NULL
        OR title LIKE CONCAT('%', #{kw}, '%')
        OR description LIKE CONCAT('%', #{kw}, '%')
        OR highlight_summary LIKE CONCAT('%', #{kw}, '%') )
ORDER BY quality_score DESC, id DESC      -- 兜底也优先高质量分案例
LIMIT #{limit}
```

- `kw` 由 `query` 取**主关键词**（先用最简单策略：去标点后整串 LIKE；分词/多词打分留给 5.2b 查询理解，不在 5.2a 过度设计）。
- `query` 为空或无命中 → 回退“最新若干条高质量案例”，让调用方判断“证据不足”而非空手。
- 排序用 `quality_score DESC`：5.1c 的质量分在兜底排序里直接发挥价值。

> 简单优先：5.2a 的 SQL 兜底刻意**不做**中文 2/3-gram 打分（反馈侧那套）。父表是“案例卡片”粒度、量级小，整串 LIKE + 质量分排序已够用；要更细的关键词召回，等 5.2d 原生 BM25 一步到位，避免在兜底里堆砌注定被替换的临时分词逻辑。

### 4.5 对外接口

```text
POST /api/knowledge/reference-videos/search        # 案例库检索（5.2a 新增）
```

**请求体 `ReferenceVideoSearchRequest`（Jakarta Validation）**

| 字段 | 类型 | 校验 | 说明 |
|---|---|---|---|
| query | String | `@NotBlank` `@Size(max=500)` | 自然语言查询 |
| category | String? | `@Size(max=64)` | 可选分区过滤 |
| tier | String? | `@Size(max=16)` | 可选层级过滤（大写归一 + 白名单，非法 → 400） |
| topK | Integer? | `@Min(1)` `@Max(50)` | 返回上限，空时用 `knowledge.rag.topK`(默认 8) |

**响应体 `ReferenceVideoSearchResponse`**：`{ mode, items: ReferenceVideoSearchItem[] }`
`ReferenceVideoSearchItem`：`videoId / bvId / tier / category / title / qualityScore / highlightSummary / embeddingStatus`（父表展示字段子集；相似度分数 5.2a 先不强求，Spring AI `similaritySearch` 默认按相似度倒序返回，顺序即相关性）。

**错误约定**：`query` 空/超长、`tier` 非法 → 400（`ResponseStatusException`，沿用现有风格）。RAG 关 / 向量库未就绪**不报错**，正常返回 `mode=SQL`（这是降级、不是错误）。

### 4.6 前端检索面板（最小可测，挂在 5.1 已有「案例库」surface）

- `KnowledgeWorkspace.vue` 顶部「案例列表」之上加一块**检索区**：查询输入框 + 分区/层级可选 + 「检索」按钮。
- 结果区复用现有案例卡片样式，额外显示一个 **`mode` chip**（语义检索 / SQL 兜底 / 语义+兜底），让作者一眼看出走了哪条路、降级是否生效。
- 客户端：`api/knowledge.ts` 加 `searchReferenceVideos(req)`；`types/knowledge.ts` 加对应类型。**不动**创作工作台侧。

### 4.7 配置键（追加到 `knowledge.rag`，复用已有前缀）

```yaml
knowledge:
  rag:
    # —— 已有：enabled / collection-name / embedding-dimension / index-batch-size / max-index-items / initialize-schema / milvus.*
    top-k: ${KNOWLEDGE_RAG_TOP_K:8}                     # 5.2a 单次检索候选上限
    min-vector-hit-count: ${KNOWLEDGE_RAG_MIN_HIT:3}    # 向量命中低于此值则合并 SQL 兜底
```

---

## 5. 后续切片技术预研（已联网核实，落地各刀时再详设）

### 5.2b 查询改写 / 多查询 / HyDE
- 纯加在检索前：用现有 LLM（Qwen/DeepSeek）把 `query` → ①改写规范化、②生成 N 条多查询、③HyDE 生成“假设答案文档”再嵌入检索。多路召回结果 Java 层去重合并。
- 不动存储；受 LLM_GUARD 输入上限保护。可配开关，关掉退回 5.2a 单查询。

### 5.2c 子表向量化 + 父子召回（small-to-big）
- 子表新增向量化：把 `creator_reference_video_item` 的优质短文本嵌入（child = small，精确召回），`embedding_*` 字段 5.1 已预留。
- 召回链：先 child 语义召回命中的子条目 → 按 `video_id` 扩展回父表案例卡片（big，富上下文）→ 父级去重。
- 子表向量入哪个集合：与父表分集合 or 同集合带 `docType` 字段——落地时定（倾向独立 child 集合，隔离清晰）。

### 5.2d 原生混合检索（dense + sparse BM25）—— 最重的一刀
**已核实（milvus-sdk-java 2.5.8 + Milvus 2.5 文档）**：
- BM25 全文检索是 **Milvus 2.5+ 服务端能力**（Standalone 支持；Milvus Lite 不支持）。
- Schema 需：`VarChar(enableAnalyzer=true)` 原文字段 + `SparseFloatVector` 字段 + `Function(FunctionType.BM25, text→sparse)` + dense `FloatVector` 字段。
- 检索：每字段一个 `AnnSearchReq`（dense 用 `FloatVec`，sparse 用原文 `EmbeddedText` 交服务端转 BM25 稀疏向量），`HybridSearchReq` + `RRFRanker`/`WeightedRanker`，`client.hybridSearch(...)`。
- **关键代价（必须让作者知情）**：Spring AI 的 `MilvusVectorStore` 按它**固定 schema** 建集合，**没有** sparse 字段与 BM25 Function。上原生 hybrid 必须把知识库集合**改成自建 schema**，并用 `KnowledgeVectorStore` 内部已持有的 raw `MilvusServiceClient`（或引入 `MilvusClientV2`）建集合 + 写入 + `hybridSearch`，即**替换 5.1c 的索引写法**。集合需重建并重灌——MySQL 是事实源，重灌无数据损失（`index/rebuild` 链路已具备）。

### 5.2e Rerank（重排序）
- 用重排模型对“混合 + 父子”后的候选精排。候选模型：DashScope `gte-rerank`（与现有 Qwen 同生态、同 Key）——**落地前需联网核实**模型名、OpenAI 兼容/DashScope 原生调用方式与限额。
- Spring AI 1.1.4 是否有一等 reranker 抽象待核实；大概率直接 HTTP 调 DashScope rerank。可配开关，关掉退回 RRF/质量分排序。

---

## 6. 必须先验证（不凭记忆，各刀编码前核实）

1. **【阻塞 5.2d】Milvus 服务端版本**：作者 VM（192.168.40.128）的 Milvus Standalone 是否 **≥ 2.5**？低于 2.5 无原生 BM25 Function，混合检索方案需改（退回 Java 层 RRF 或先升级 Milvus）。**请作者确认当前 Milvus 版本。**
2. **【5.2a 即用】** Spring AI `MilvusVectorStore.similaritySearch` + `FilterExpressionBuilder` 对 `category/tier` 元数据过滤在知识库集合上的实际可用性（反馈侧按 `taskId` 已验证，预期可用；自测时确认）。
3. **【5.2d】** milvus-sdk-java 2.5.8 的 `hybridSearch` 走 v1（`io.milvus.client`，5.1 现持）还是 v2（`io.milvus.v2.client.MilvusClientV2`）API，及与现有 v1 client 共存方式。
4. **【5.2e】** DashScope 重排模型（`gte-rerank` 等）的真实可用名、调用通道与限额。

---

## 7. 实施子步骤（一次只解决一个问题）

本次仅 **5.2a**，建议拆为：
1. 配置键 + mapper 两个查询（回查 / SQL 兜底）+ `KnowledgeRetrievalService`（dense + 兜底 + 模式），先 curl 自测后端。
2. `/search` 接口 + 入参校验 + 响应组装。
3. 前端检索面板 + `mode` chip。

5.2b–e 各自独立成刀，落地时再按本文件 §5 出详设。

---

## 8. 测试验证点（编译 / 运行由作者执行）

- **降级（RAG 关，默认）**：`POST /search {"query":"开场怎么做"}` → `mode=SQL`，返回关键词命中或最新高质量案例；向量库不连、不调 Embedding、不报错。
- **语义（开 Qwen v4 + Milvus 且父表已索引）**：同查询 → `mode=VECTOR`，候选按相似度排序；命中 < `min-vector-hit-count` 时 → `mode=VECTOR_WITH_SQL_FALLBACK`，向量结果在前、SQL 补后、去重。
- **过滤**：带 `category`/`tier` → 只返回该分区/层级；`tier` 非法 → 400。
- **边界**：`query` 空/超长 → 400；库内无案例 → 返回空 `items` 且不报错。
- **隔离**：知识库检索不触碰反馈侧 `ObjectProvider<VectorStore>`，反馈链路解析不受影响。

---

## 9. 本阶段不做

Agent 工具化与接入发布前优化/竞品（5.3）、知识库完整管理后台（5.4）、跨创作者权限隔离、多模态视频内容评分。5.2b–e 的查询改写/父子/原生 hybrid/Rerank 按切片在后续逐刀落地，不在 5.2a。

---

## 10. 5.2a 实施记录（已完成 · 2026-06-07）

第一刀 5.2a 已编码并由作者自测通过（后端 curl + 前端面板）。本节记录**实际落地**与 §4 设计的差异——§1–§9 设计正文保留原貌，差异统一在此说明，便于看清「设计 → 实施」的收敛过程。

### 10.1 实际落地清单

**后端**（作者编译 + 4 条 curl 自测通过）：

| 文件 | 动作 | 说明 |
|---|---|---|
| `knowledge.model.ReferenceVideoSearchRequest` | 新增 record | `query`(@NotBlank/@Size 500) + `category?`/`tier?`/`topK?`(@Min1/@Max50) |
| `knowledge.model.ReferenceVideoSearchResponse` | 新增 record | `{ mode, items }`；items **复用 `ReferenceVideoResponse`**（见偏差①） |
| `knowledge.model.ReferenceVideoResponse` | 改动 | 加静态工厂 `from(record)`，检索/列表共用 21 字段组装 |
| `knowledge.service.KnowledgeReferenceRetrievalService` | 新增 Service | 三段式：dense → 回查父表 → SQL 兜底（类名见偏差②） |
| `knowledge.mapper.KnowledgeReferenceVideoMapper` | 改动 | 加 `listByVideoIds`(回查事实源) + `searchByKeyword`(SQL 兜底) |
| `knowledge.controller.KnowledgeReferenceVideoController` | 改动 | 加 `POST /search`（第 4 个构造依赖） |
| `knowledge.config.KnowledgeRagProperties` | 改动 | 加 `topK`=8 / `minVectorHitCount`=3（见偏差③） |

**前端**（作者 `npm run dev` 自测通过）：

| 文件 | 动作 | 说明 |
|---|---|---|
| `types/knowledge.ts` | 改动 | 加 `ReferenceVideoSearchPayload` / `ReferenceVideoSearchResult` |
| `api/knowledge.ts` | 改动 | 加 `searchReferenceVideos`（POST `/search`，空过滤项不下发） |
| `components/KnowledgeWorkspace.vue` | 改动 | 「案例检索」区块（插在*向量索引*与*案例列表*之间）+ 三态 mode 标签 + 复用案例卡 |

### 10.2 与 §4 设计的偏差（均为「简单优先」收敛，非功能缩水）

1. **不新建 `ReferenceVideoSearchItem`，直接复用 `ReferenceVideoResponse`**：§4.2/§4.5 原设计单列候选 record。落地发现检索结果与列表是**同一种案例卡片**、字段完全一致，再造一个几乎同形的 record 是冗余抽象。改为给 `ReferenceVideoResponse` 加 `from(record)` 工厂、检索与列表共用。相似度分数未单列——Spring AI `similaritySearch` 默认按相似度倒序，**顺序即相关性**，5.2a 不强求显式分值。
2. **Service 名 `KnowledgeReferenceRetrievalService`（非 §4.2 的 `KnowledgeRetrievalService`）**：与同模块 `KnowledgeReference{Video,Index,Fetch}Service` 命名一致，点明它专检索「reference video」案例库，避免与未来可能的通用检索撞名。
3. **配置键 `top-k`/`min-vector-hit-count` 未写入 application.yml**：仅在 `KnowledgeRagProperties` 用 Java 默认值（topK=8 / minHit=3），与 `max-index-items` 同款处理——零配置即合理默认；需调再补 yml 绑定。§4.7 的 yml 片段属「可选暴露」，本刀未启用。

### 10.3 自测结果（作者执行）

- **后端 4 条 curl**：默认 `mode=SQL` 200 / 空 query 400 / 非法 tier 400 / `category` 过滤 + `topK` 截断 —— 全绿。
- **前端 4 场景**：默认降级标签「SQL 关键词检索」、空查询按钮置灰、过滤生效、后端停机红字兜底 —— 全绿。
- **降级红线达成**：RAG 默认关时不连 Milvus、不调 Embedding、不报错，`mode=SQL` 正常返回。

### 10.4 下一刀

5.2b（查询改写 / 多查询 / HyDE）或 5.2c（子表向量化 + 父子召回）—— **作者已拍板先做 5.2b，且要「三种都做、可切换」**，详设见 §11。5.2d 原生 hybrid 仍**阻塞于** Milvus 服务端版本确认（见 §6.1）。

---

## 11. 5.2b 详细设计（查询理解 · 三策略可切换，待作者自检）

> 作者拍板（2026-06-07）：**rewrite / HyDE / multi-query 三种都做、可切换**。本节按此设计，但坚持「统一抽象 + 朴素合并」避免过度设计：三策略统一收敛为「把原始 query 变成 1~N 条检索文本」，正式 RRF 融合仍留 5.2d。

### 11.1 共性约束（三策略通用，先立规矩）

1. **只增强向量路径**：扩展产出的文本仅用于 dense 检索；**SQL 兜底始终用原始 query**（扩展串会让字面 LIKE 更难命中）。
2. **默认开启增强**：配置 `strategy` 默认 **`REWRITE`**（单路、最稳的增强）；`NONE` 仅作显式关闭 / 对照保留。RAG 关时向量路径不走，增强自然不触发。
3. **失败必降级**：扩展异常/超时/空结果 → 退回 `[原始 query]` 单路检索，**绝不让扩展失败中断检索**（照搬 5.2a「向量失败退 SQL」的容错哲学）。
4. **成本与护栏**：查询变换由 Spring AI 组件经 `ChatClient` 调 LLM（**绕过 `LLMService.validatePromptLength`**），但 query 受入参 `@Size(500)` 约束、远低于 30000 字符护栏，无实际影响；`MultiQueryExpander.numberOfQueries` 另设硬上限防检索次数放大。

### 11.2 统一抽象（关键设计：三策略 → 同一形状）

三种手段本质都是「原始 query → 1~N 条用于 dense 检索的文本」：

| 策略 | LLM 产出 | 返回检索文本数 |
|---|---|---|
| `NONE` | 不调 LLM | 1（原始 query，等于 5.2a） |
| `REWRITE` | 规范化/补词后的查询 | 1 |
| `HYDE` | 一段假设的优质视频案例描述 | 1 |
| `MULTI_QUERY` | N 条不同角度查询（`MultiQueryExpander` 产出）+ 并入原 query | ≤ `multi-query-count`（默认 3）+1 |

→ 统一接口 **`List<String> enhance(String query, QueryEnhanceStrategy strategy)`**；检索层只管「拿到 1~N 条文本、逐条检索、合并去重」，不关心策略。新增策略只加一个 `case`，不碰检索层。

### 11.3 包与类（基于 Spring AI 原生 Modular RAG 查询组件）

> 作者拍板用框架原生组件（非手写 prompt）。需新增依赖 `org.springframework.ai:spring-ai-rag`（版本由项目已引入的 spring-ai-bom 统一管理，1.1.x 含本组件；该 RAG 模块属实验性、API 可能随版本变——已知情接受）。

| 新增/改动 | 类型 | 职责 |
|---|---|---|
| `knowledge.model.QueryEnhanceStrategy` | 新增 enum | `NONE / REWRITE / HYDE / MULTI_QUERY` |
| `knowledge.rag.HydeQueryTransformer` | 新增 | 自定义实现 `QueryTransformer`（框架无内置 HyDE）：`transform(Query)` 内用 `ChatClient` 按中文 prompt 生成假设案例描述，返回 `new Query(假设文档)` |
| `knowledge.service.KnowledgeQueryEnhancer` | 新增 Service | 构造注入 `ChatClient.Builder`，持有 `RewriteQueryTransformer` / `MultiQueryExpander` / `HydeQueryTransformer`；`enhance(query, strategy)` 按策略调对应组件、统一产出 `List<String>`；含失败降级 |
| `knowledge.service.KnowledgeReferenceRetrievalService` | 改动 | `search()` 内：先 `enhance` 得多条文本 → 逐条 `denseSearch` → 按 videoId 合并去重 → 回查/兜底/模式回显**全不变** |
| `knowledge.model.ReferenceVideoSearchRequest` | 改动 | 加可选 `strategy`（覆盖配置默认，便于逐次切换不同策略对比检索效果） |
| `knowledge.model.ReferenceVideoSearchResponse` | 改动 | 加回显 `strategy` + 可选 `enhancedQueries`（回显扩展出的查询，便于核对增强是否合理、定位召回问题） |
| `knowledge.config.KnowledgeRagProperties` | 改动 | 加 `queryEnhancement.strategy`(默认 `REWRITE`) + `multiQueryCount`(默认 3) |

> 仍放 `knowledge` 模块（跟 5.2a 一起），**不**进 `creator/search`。`KnowledgeQueryEnhancer` 把三个框架组件包成统一 `enhance(query, strategy) → List<String>`，检索层不感知框架细节；不建通用策略框架（无第二消费方），策略分发就是一个 switch。

### 11.4 三策略落地（框架组件 + 中文 prompt）

`enhance(query, strategy)` 内按策略映射到框架组件，统一取出文本：

- **REWRITE** → `RewriteQueryTransformer.builder().chatClientBuilder(cb).promptTemplate(中文模板).build().transform(new Query(q)).text()`，得 1 条。
- **MULTI_QUERY** → `MultiQueryExpander.builder().chatClientBuilder(cb).numberOfQueries(n).includeOriginal(true).promptTemplate(中文模板).build().expand(new Query(q))` → `List<Query>` 逐个取 `.text()`（`includeOriginal(true)` 由框架并入原 query，无需自己拼）。
- **HYDE** → 自定义 `HydeQueryTransformer.transform(new Query(q)).text()`：内部用 `ChatClient` 按中文 prompt 生成一段假设的优质视频「亮点摘要」（标题感 + 要点；**只描述方法/共性，禁编造具体 UP 主、播放量、BV 号**防幻觉），返回 `new Query(假设文档)`。
- 任一策略异常/空 → 在 `KnowledgeQueryEnhancer` 内 try/catch 降级返回 `[原始 query]`。

> ⚠️ **必须传中文 `promptTemplate`**：`RewriteQueryTransformer` / `MultiQueryExpander` 内置默认 prompt 是**英文**，对中文 query / 案例库会劣化甚至输出英文。故二者都显式注入中文模板；HyDE 是自定义、本就中文。

### 11.4b 编码前必须核实的 API 点（不凭记忆，对照官方）

1. `spring-ai-rag` 在项目现用 spring-ai-bom 版本里、能否仅靠 BOM 不写死版本号。
2. `Query.text()` 取值访问器名、`new Query(String)` 构造可用。
3. `RewriteQueryTransformer` / `MultiQueryExpander` 的 `promptTemplate(..)` 参数类型与**模板占位符变量名**（中文模板要用对变量名，否则注入失败）。
4. `MultiQueryExpander` 的 `includeOriginal(..)` / `numberOfQueries(..)` 确实存在。
5. 这些组件入参是 `ChatClient.Builder`——确认可直接注入（项目 `LLMService` 已用 `ChatClient.Builder` 构造，bean 存在）。

### 11.5 检索编排改造（5.2a `search()` 的最小侵入）

```
search(request):
  query = 原始 query；strategy = request.strategy ?? 配置默认
  ...（RAG 关 / 向量库不可用 → SQL 兜底，用原始 query，不变）

  // 唯一新增：单 query → 多 query
  List<String> searchTexts = (strategy == NONE) ? [query]
                             : queryEnhancer.enhance(query, strategy)   // 内部失败已退回 [query]

  // 逐条 denseSearch，按 videoId 合并去重（保留首次出现 / 最高名次），取前 topK
  orderedVideoIds = mergeDistinct( searchTexts.map(t -> denseSearch(t, category, tier, topK)) )

  ...（回查父表 is_deleted=0、命中不足合并 SQL 兜底、mode 回显 —— 全部复用 5.2a，零改动）
```

- **合并去重**：朴素策略——按 `searchTexts` 顺序拼接各自 videoId 列表，`LinkedHashSet` 去重，取前 topK。**不做 RRF 加权**（正式 RRF 留 5.2d）。
- **mode 不变**：仍 VECTOR / SQL / VECTOR_WITH_SQL_FALLBACK；`strategy` 单独回显。
- **成本**：multi 会发 N+1 次 dense 检索（每条 query 各 1 次 embed + 1 次向量库往返），故 `multiQueryCount` 设硬上限；rewrite/HyDE 仍是单路（多 1 次 LLM 而已）。

### 11.6 配置 + API

```yaml
knowledge:
  rag:
    query-enhancement:
      strategy: ${KNOWLEDGE_QE_STRATEGY:REWRITE}    # NONE/REWRITE/HYDE/MULTI_QUERY，默认 REWRITE
      multi-query-count: ${KNOWLEDGE_QE_MULTI_N:3}  # MULTI_QUERY 上限，二次防御
```

- `POST /search` 请求体加可选 `strategy`（Service 内枚举大写归一 + 白名单校验，非法 → 400，沿用 tier 口径）；为空用配置默认。

### 11.7 前端（最小增量，挂在 5.2a 检索面板）

- 检索 toolbar 加**策略下拉**：默认 / 查询改写 / HyDE / 多查询。
- 结果区已显示 `mode`；再加显示**本次 strategy** +（可选）一行「扩展出的查询：…」，让调用方能核对 LLM 实际扩展出的查询、评估增强质量、排查召回偏差。
- `api/knowledge.ts` 的 `searchReferenceVideos` payload 加可选 `strategy`；`types/knowledge.ts` 加枚举与回显字段。

### 11.8 测试验证点（编译/运行由作者执行）

- **默认 REWRITE**：开 RAG 时默认即走改写增强；`strategy=NONE` 时行为与 5.2a 完全一致（回归不破）。
- **REWRITE/HYDE（开 RAG + 已索引）**：`strategy` 回显 + `enhancedQueries` 可见；命中较 NONE 有变化。
- **MULTI_QUERY**：扩出 ≤N 条 + 含原 query；多路结果去重合并、不超 topK。
- **失败降级**：LLM 网关不可用 → `KnowledgeQueryEnhancer` 内部兜底退回原 query，检索不报错、`mode` 正常。
- **SQL 路径**：RAG 关时无论 strategy 取何值，都走原始 query 的 SQL 兜底（strategy 对兜底零副作用）。
- **中文 prompt**：REWRITE / MULTI_QUERY 用中文 `promptTemplate`，验证扩展输出是中文（非框架默认英文）。

### 11.9 本刀不做

- 正式 RRF / 加权融合（→ 5.2d，与原生 hybrid 一起）。
- Prompt 版本化（项目 4.9 是评测归档、非运行时路由；继续硬编码）。
- 扩展查询的缓存 / 限流（暂不需要；真有高频重复查询再加 Redis）。

### 11.10 实施子步骤（一次一个问题）

1. `QueryEnhanceStrategy` enum + `KnowledgeRagProperties` 两个配置项。
2. 加 `spring-ai-rag` 依赖 +（编码前核实 §11.4b）→ `HydeQueryTransformer`（自定义 `QueryTransformer`）+ `KnowledgeQueryEnhancer`（组合三框架组件 + 中文 `promptTemplate` + 失败降级），先手测 enhance 产出。
3. 改 `KnowledgeReferenceRetrievalService.search()`：多路检索 + 合并去重；改 Request/Response（strategy + enhancedQueries 回显）。curl 自测。
4. 前端策略下拉 + 回显。
5. 文档补「5.2b 实施记录」。

---

## 12. 5.2b 实施记录（已完成 · 2026-06-07）

5.2b（查询理解 · 三策略可切换）已按 §11 编码、作者分步自测通过（后端编译 + curl，前端 `npm run dev`）。

### 12.1 实际落地清单

**后端**（§11.10 子步 1→3）：

| 文件 | 动作 | 说明 |
|---|---|---|
| `pom.xml` | 改动 | 加 `spring-ai-rag`（无版本，BOM 管 1.1.4，见 §12.3） |
| `knowledge.model.QueryEnhanceStrategy` | 新增 enum | `NONE / REWRITE / HYDE / MULTI_QUERY` |
| `knowledge.config.KnowledgeRagProperties` | 改动 | 加 `QueryEnhancement` 嵌套：`strategy`(默认 `REWRITE`) + `multiQueryCount`(默认 3) |
| `knowledge.rag.HydeQueryTransformer` | 新增 | 自定义 `QueryTransformer`：`ChatClient` 生成中文假设文档，失败原样返回 |
| `knowledge.service.KnowledgeQueryEnhancer` | 新增 Service | 组合 `RewriteQueryTransformer` / `MultiQueryExpander` / `HydeQueryTransformer`（中文 `promptTemplate`），`enhance(query,strategy)→List<String>` + 三层降级 |
| `knowledge.service.KnowledgeReferenceRetrievalService` | 改动 | 注入 enhancer；`search()` 接 `resolveStrategy` + `denseSearchMulti`；4 处响应改 4 参 |
| `knowledge.model.ReferenceVideoSearchRequest` | 改动 | 加可选 `strategy`（`@Size(max=16)`，Service 内枚举校验非法 400） |
| `knowledge.model.ReferenceVideoSearchResponse` | 改动 | 4 字段：`mode` / `strategy` / `enhancedQueries` / `items` |

**前端**（子步 4）：`types`（payload 加 `strategy`、result 加 `strategy`/`enhancedQueries`）、`api`（空 `strategy` 不下发）、`KnowledgeWorkspace.vue`（策略下拉 + `strategyLabel` + 「增强策略」标签 + 扩展查询回显）。

### 12.2 关键实现决策（§11 的细化）

1. **原生组件 + 自定义 HyDE**：三策略统一在 `enhance()→List<String>`，检索层不感知；`MultiQueryExpander.includeOriginal(true)` 由框架并入原 query。
2. **中文 `promptTemplate` 占位符锁定**：对 1.1.4 字节码核准——Rewrite=`{query}`/`{target}`、Multi=`{query}`/`{number}`，改名会渲染报错；`targetSearchSystem` 设中文「视频案例知识库」。
3. **`strategy` 回显「实际生效」**：走 SQL 兜底（RAG 关 / 向量库不可用 / 向量异常）时回显 `NONE` + 空 `enhancedQueries`，与 `mode` 一致。
4. **多查询朴素去重**：`denseSearchMulti` 单条短路走原 `denseSearch`；多条逐条检索 + `LinkedHashSet` 保序去重取 topK，**无 RRF**（留 5.2d）。
5. **三层失败降级**：HyDE 内部 catch → `enhance` 外层 catch → `search` 的 `isEmpty()` 兜底；`denseSearchMulti` 抛则整体退 SQL。
6. **配置走 Java 默认未入 yml**：`strategy=REWRITE`/`multiQueryCount=3`（与 5.2a `topK` 一致）；运行时切策略走 API `strategy`。

### 12.3 版本核实插曲（踩坑提醒）

`pom.xml` 加 `spring-ai-rag` 无版本后，反编译 `.m2` 一度看到 **1.0.0**，疑似与项目 1.1.4 混版本。核实 `spring-ai-bom-1.1.4.pom`（第 89-90 行）确认 **BOM 管理 `spring-ai-rag = ${project.version}`（即 1.1.4）**；`.m2` 的 1.0.0 只是旧缓存、`find | head -1` 撞上了它。**结论：无版本声明正确，实际编译用 1.1.4，pom 不必锁版本。** 所用 API 已对 1.1.4 字节码逐一核准。

### 12.4 自测结果（作者执行）

- 后端 curl：默认 `mode=SQL`/`strategy=NONE`、请求 HYDE 但 RAG 关→`NONE`、非法 strategy→400、小写归一→200 —— 全绿。
- 前端：策略下拉、「增强策略」回显、RAG 关如实显示「未增强」、`vue-tsc` 无错 —— 全绿。
- **REWRITE/HYDE/MULTI_QUERY 真实增强效果**：需开 RAG + Milvus + LLM 验证；当前只验证了降级 / 校验 / 回显链路。

### 12.5 下一刀

5.2c（子表向量化 + 父子召回）或 5.2e（Rerank）由作者拍板；5.2d 原生 hybrid 仍阻塞于 Milvus 版本确认（§6.1）。查询增强与后续刀正交，可叠加。
