# 阶段 4.0 - MVC 架构重整踩坑记录

## 1. 包名和文件路径必须同时改

只改 `package` 声明不够，文件路径也要一起移动，不然后面很容易出现“目录看起来对了，编译器找不到类”的问题。

## 2. 旧引用会散落在测试里

主代码改完后，测试代码也要重新扫一遍。很多编译错误不是业务逻辑错，而是测试还在引用旧包名。

## 3. 先收口再扩展

这次只做 MVC 分层，不顺手引入额外 DTO 转换器、Repository 抽象或通用基类。先把边界稳定住，后面再按需要补。
